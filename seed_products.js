const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const brand = await prisma.brand.upsert({ where: { slug: 'test-brand' }, update: {}, create: { name: 'Test Brand', slug: 'test-brand' } }); const user = await prisma.user.findFirst(); const collection = await prisma.collection.findFirst({ where: { userId: user.id } }); console.log('User:', user.id, 'Collection:', collection.id); const product = await prisma.product.create({ data: { title: 'Lüks Deri Çanta', price: 1200, currency: 'EUR', brandId: brand.id, imageUrl: 'https://images.unsplash.com/photo-1548036328-c9fa89d128fa?w=500&q=80', productUrl: 'https://example.com/bag', } }); await prisma.collectionItem.create({ data: { collectionId: collection.id, productId: product.id, curatorComment: 'Bu çanta inanılmaz kaliteli ve her şeyle uyumlu! Kesinlikle tavsiye ederim.' } }); console.log('Created product:', product.id); } main();
